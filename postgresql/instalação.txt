* sudo apt install postgresql

* sudo apt install pgadmin3

* Conectar RAILS com Postgresql
    * sudo apt install libpq-dev

* Desabilitar AutoStart serviço
    * sudo update-rc.d -f postgresql defaults-disabled
    * sudo update-rc.d -f postgresql disable
    * Executei os dois comandos, não sei qual desabilitou exatamente
