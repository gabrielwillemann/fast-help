* Comando para mostrar todas as variáveis de ambiente:
    * env
    * printenv
    * printenv | sort
    

* Comando para mostrar uma variável de ambiente:
    * printenv PATH
    * echo $PATH
    * env | grep MY_VAR

* Comando para buscar/filtrar uma variável de ambiente
    * printenv | grep -i MY_VAR

* Adicionar variavel de ambiente
    * export MY_VAR=X

* Variável permanente, inserir em um destes arquivos:
    * /etc/profile
    * /etc/profile.d/
    * /etc/bash.bashrc
    * /etc/environment

